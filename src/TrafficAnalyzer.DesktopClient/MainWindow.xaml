<Window
    x:Class="TrafficAnalyzer.DesktopClient.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:canvas="using:Microsoft.Graphics.Canvas.UI.Xaml">

	<Grid Background="#0d1117" Padding="15">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<!-- Top Bar -->
		<Grid Grid.Row="0" Margin="0,0,0,15">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*"/>
				<ColumnDefinition Width="Auto"/>
			</Grid.ColumnDefinitions>

			<!-- Legend -->
			<StackPanel Orientation="Horizontal" Spacing="20" VerticalAlignment="Center">
				<StackPanel Orientation="Horizontal" Spacing="8">
					<Rectangle Width="14" Height="14" Fill="#58a6ff" RadiusX="2" RadiusY="2"/>
					<TextBlock Text="TCP" Foreground="#c9d1d9"/>
				</StackPanel>
				<StackPanel Orientation="Horizontal" Spacing="8">
					<Rectangle Width="14" Height="14" Fill="#3fb950" RadiusX="2" RadiusY="2"/>
					<TextBlock Text="UDP" Foreground="#c9d1d9"/>
				</StackPanel>
				<StackPanel Orientation="Horizontal" Spacing="8">
					<Rectangle Width="14" Height="14" Fill="#e3b341" RadiusX="2" RadiusY="2"/>
					<TextBlock Text="ICMP" Foreground="#c9d1d9"/>
				</StackPanel>
				<StackPanel Orientation="Horizontal" Spacing="8">
					<Rectangle Width="14" Height="14" Fill="#bc8cff" RadiusX="2" RadiusY="2"/>
					<TextBlock Text="Other IP" Foreground="#c9d1d9"/>
				</StackPanel>
				<StackPanel Orientation="Horizontal" Spacing="8">
					<Rectangle Width="14" Height="14" Fill="#ff7b72" RadiusX="2" RadiusY="2"/>
					<TextBlock Text="Non-IP (ARP)" Foreground="#c9d1d9"/>
				</StackPanel>
			</StackPanel>

			<!-- Stats -->
			<Border Grid.Column="1" Background="#1f2428" CornerRadius="6" Padding="16,8">
				<StackPanel Orientation="Horizontal" Spacing="15">
					<TextBlock Foreground="#8b949e">
						<Run Text="Packets/s: "/>
						<Run x:Name="PpsValue" Text="0" Foreground="#58a6ff" FontWeight="Bold"/>
					</TextBlock>
					<TextBlock Foreground="#8b949e">
						<Run Text="Batches/s: "/>
						<Run x:Name="BpsValue" Text="0" Foreground="#3fb950" FontWeight="Bold"/>
					</TextBlock>
					<TextBlock Foreground="#8b949e">
						<Run Text="Lag: "/>
						<Run x:Name="LagValue" Text="0" Foreground="#ff7b72" FontWeight="Bold"/>
						<Run Text="ms"/>
					</TextBlock>
				</StackPanel>
			</Border>
		</Grid>

		<!-- Hardware Accelerated Canvas -->
		<canvas:CanvasControl x:Name="GridCanvas" Grid.Row="1"
                              Draw="GridCanvas_Draw"
                              SizeChanged="GridCanvas_SizeChanged"
                              CornerRadius="2" />
	</Grid>
</Window>